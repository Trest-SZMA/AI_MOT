# Расчёт и отчёты
openpyxl>=3.1
# Веб-приложение (загрузка выгрузок и пересборка через браузер)
fastapi>=0.110
uvicorn>=0.27
# Источники напрямую из MS SQL («Extractor», куда 1С складывает таблицы).
# ⚠️ ОСНОВНОЙ ДРАЙВЕР — python-tds, чистый Python. Он кодирует пароль в UTF-16
# по спецификации TDS, как это делает JDBC-драйвер Microsoft. Это принципиально:
# pymssql (обёртка над FreeTDS) с не-ASCII паролем получает отказ входа 18456
# при верном пароле — проверено на всех кодировках.
python-tds>=1.15
# Запасной: отработает ASCII-пароль, если python-tds почему-то не встанет.
pymssql>=2.3
